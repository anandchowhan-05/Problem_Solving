        for(int i=nums.length-2;i>=0;i--){
            b[i]=b[i+1]+nums[i+1];
        }
        for(int i=0;i<nums.length;i++){
            if(a[i]==b[i]){
                return i;
            }
        }
    }
        return -1;
}
